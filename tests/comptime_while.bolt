extern write(fd : i32, buffer : *u8, count : u64) : u64

print(str : []u8) : i32 {
    write(1, &str[0], len(str))
    return 0
}

@hi := "Gi!"

test() : i32 {
    @i := 0
    @while i < 42 {
        print(hi)
        i = i + 1
    }
    return 0
}

main() : i32 {
    test()
    return 0
}

