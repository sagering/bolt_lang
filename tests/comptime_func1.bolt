@List(capacity : u32, T: type) : type {
    return struct {
        items : [capacity]T
        size  : u32
    }
}

main() : i32 {
    p := List(10, i32) {}
    q := List(10, i32) {}
    p = q
    return 0
}
