struct ABC {
    a : i64
    b : i64
    c : i64
}

struct E {
    e : i32
}

use_struct ( input : *ABC ) : ABC {
    c : ABC = ABC {}
    return c
}

use_struct1 ( input : *[3]ABC ) : ABC {
    c : ABC = ABC {}
    return c
}

use_struct2 ( input : *[]ABC ) : ABC {
    c : ABC = ABC {}
    return c
}

use_struct3 ( input : [2]ABC ) : ABC {
    c : ABC = ABC {}
    return c
}

my_function( a : i64 ) : bool {
    b : i64 = 1
    return a * b == 3
}

funky( c : i64 ) : i64 {

    d : ABC = ABC {}

    if c == -1 {
        return 0
    }

    while true {
        break
    }

    return -1
}

main() : i32 {
    funky( 938 )

    a : ABC = ABC{}
    b : E = E{}

    if my_function( 3 ) {
        return b.e
    }

    return b.e
}
